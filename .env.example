# Nuxt 运行时配置
# 网站URL（用于生产环境）
NUXT_PUBLIC_SITE_URL=https://home.zxcoder.top

# RSS配置
# RSS/Atom订阅源URL（必填项，用于文章页面）
# 支持 RSS 和 Atom 两种格式
# 如果不设置，将使用 nuxt.config.ts 中的默认值（https://blog.mcyzsx.top/atom.xml）
# 如果原始URL超时，系统会自动尝试以下降级方案：
# 1. 尝试使用HTTP而非HTTPS
# 2. 在开发环境下尝试使用CORS代理服务
RSS_URL=https://blog.mcyzsx.top/atom.xml

# 备选RSS源（如果主要源经常超时，可尝试以下源）：
# RSS_URL=https://blog.sakura.ink/rss.xml
# RSS_URL=https://hakurei.cc/rss.xml
# RSS_URL=https://zsd.name/rss.xml
# RSS_URL=https://blog.example.com/feed.xml

# 注意：
# 1. 确保URL包含协议前缀（http:// 或 https://）
# 2. 如果遇到超时问题，可能是网络问题或服务器响应缓慢
# 3. 建议选择响应速度较快、稳定性较好的RSS源
# 4. 可以使用 https://www.feedvalidator.org/ 检查RSS源是否有效

# SMTP配置

# SMTP 服务器地址
SMTP_HOST=smtp.qq.com

# SMTP 端口(不填写默认465）
SMTP_PORT=465

# SMTP 用户名（一般与SENDER_EMAIL相同）
SMTP_USER=3149261770@qq.com

# SMTP 密码
SMTP_PASS=password

# 收件邮箱地址
ADMIN_EMAIL=3149261770@qq.com

# 发件邮箱地址（一般与SMTP_USER相同）
SENDER_EMAIL=3149261770@qq.com

# 是否启用SSL
SMTP_SECURE=true